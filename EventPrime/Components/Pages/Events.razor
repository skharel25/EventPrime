@page "/events"

<PageTitle>Events – EventPrime</PageTitle>

<!-- Page Banner -->
<section class="page-banner">
    <div class="banner-shapes">
        <div class="banner-shape bs-1"></div>
        <div class="banner-shape bs-2"></div>
    </div>
    <div class="container banner-body">
        <div class="section-tag">Discover</div>
        <h1 class="banner-title">All Events</h1>
        <p class="banner-subtitle">Browse hundreds of curated events happening near you and around the world.</p>
    </div>
</section>

<!-- Filter Bar -->
<section class="filter-bar-section">
    <div class="container">
        <div class="filter-bar">
            <div class="filter-group">
                <label class="filter-label">Category</label>
                <select class="filter-select">
                    <option>All Categories</option>
                    <option>Music</option>
                    <option>Business</option>
                    <option>Arts</option>
                    <option>Sports</option>
                    <option>Food</option>
                    <option>Education</option>
                </select>
            </div>
            <div class="filter-group">
                <label class="filter-label">Location</label>
                <select class="filter-select">
                    <option>All Locations</option>
                    <option>New York</option>
                    <option>Los Angeles</option>
                    <option>Chicago</option>
                    <option>Houston</option>
                </select>
            </div>
            <div class="filter-group">
                <label class="filter-label">Date</label>
                <select class="filter-select">
                    <option>Any Date</option>
                    <option>This Week</option>
                    <option>This Month</option>
                    <option>Next 3 Months</option>
                </select>
            </div>
            <button class="btn-filter-apply">Apply Filters</button>
        </div>
    </div>
</section>

<!-- Events Listing -->
<section class="events-listing-section">
    <div class="container">
        <h2 class="listing-title">Upcoming Events <span class="listing-count">No events yet</span></h2>
        <div class="listing-grid">

            <div class="listing-card listing-card-empty">
                <div class="empty-listing-inner">
                    <span class="empty-listing-icon">🗓️</span>
                    <p class="empty-listing-text">Nothing here yet</p>
                    <span class="empty-listing-sub">Events will appear here once added</span>
                </div>
            </div>

            <div class="listing-card listing-card-empty">
                <div class="empty-listing-inner">
                    <span class="empty-listing-icon">🗓️</span>
                    <p class="empty-listing-text">Nothing here yet</p>
                    <span class="empty-listing-sub">Events will appear here once added</span>
                </div>
            </div>

            <div class="listing-card listing-card-empty">
                <div class="empty-listing-inner">
                    <span class="empty-listing-icon">🗓️</span>
                    <p class="empty-listing-text">Nothing here yet</p>
                    <span class="empty-listing-sub">Events will appear here once added</span>
                </div>
            </div>

            <div class="listing-card listing-card-empty">
                <div class="empty-listing-inner">
                    <span class="empty-listing-icon">🗓️</span>
                    <p class="empty-listing-text">Nothing here yet</p>
                    <span class="empty-listing-sub">Events will appear here once added</span>
                </div>
            </div>

            <div class="listing-card listing-card-empty">
                <div class="empty-listing-inner">
                    <span class="empty-listing-icon">🗓️</span>
                    <p class="empty-listing-text">Nothing here yet</p>
                    <span class="empty-listing-sub">Events will appear here once added</span>
                </div>
            </div>

            <div class="listing-card listing-card-empty">
                <div class="empty-listing-inner">
                    <span class="empty-listing-icon">🗓️</span>
                    <p class="empty-listing-text">Nothing here yet</p>
                    <span class="empty-listing-sub">Events will appear here once added</span>
                </div>
            </div>

        </div>

        <div class="pagination-bar" style="display:none;">
            <button class="pg-btn active">1</button>
            <button class="pg-btn">2</button>
            <button class="pg-btn">3</button>
            <button class="pg-btn pg-next">Next →</button>
        </div>
    </div>
</section>
